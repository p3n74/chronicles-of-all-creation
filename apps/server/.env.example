# Local API (Vite web runs on :3001)
CORS_ORIGIN=http://localhost:3001
NODE_ENV=development

# Production / Coolify (same public origin as the site):
# CORS_ORIGIN=https://chronicles.example.com
# NODE_ENV=production
# PORT=3000
# HOSTNAME=0.0.0.0
